config:
  target: "http://httpbin.org/"
  phases:
    - duration: 20
      arrivalRate: 5
      name: Artillery First Test
scenarios:
  - name: Getting a resource
    flow:
      - get:
          url: "/get"
      - post:
          url: "/post"
          json:
            username: "Test User"
            password: "Temp Password"
